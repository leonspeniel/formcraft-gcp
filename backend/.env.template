# Local database settings (use the values defined below for local docker compose setup)
POSTGRES_USER=form_user
POSTGRES_PASSWORD=secure_dev_password
POSTGRES_DB=form_db
DATABASE_URL=postgresql://form_user:secure_dev_password@db:5432/form_db

# Security
JWT_SECRET=replace_this_with_a_secure_long_random_string_in_production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
